<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 经典蓝牙及 Android 11 及以下权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Android 12+ 蓝牙运行时权限 -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- 声明 BLE 特性（非强制，提高设备兼容性） -->
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />

    <application>
        <activity android:name=".BluetoothMainActivity" />

        <!-- 原生 BLE 方案 -->
        <activity android:name=".native_ble.BleNativeScanActivity" />
        <activity android:name=".native_ble.BleNativeConnectActivity" />
        <activity android:name=".native_ble.BleNativeQueueActivity" />

        <!-- Nordic BLE 方案 -->
        <activity android:name=".nordic_ble.BleNordicScanActivity" />
        <activity android:name=".nordic_ble.BleNordicConnectActivity" />
        <activity android:name=".nordic_ble.BleNordicTransferActivity" />

        <!-- FastBle 方案 -->
        <activity android:name=".fast_ble.BleFastScanActivity" />
        <activity android:name=".fast_ble.BleFastConnectActivity" />

        <!-- RxAndroidBle 方案 -->
        <activity android:name=".rx_ble.BleRxScanActivity" />
        <activity android:name=".rx_ble.BleRxConnectActivity" />
    </application>

</manifest>
