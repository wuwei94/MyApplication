<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application>
        <activity android:name=".GpuImageMainActivity" />
        <activity android:name=".filter.GpuImageFilterActivity" />
        <activity android:name=".adjust.GpuImageAdjustActivity" />
        <activity android:name=".group.GpuImageGroupActivity" />
        <activity android:name=".camera.GpuImageCameraFilterActivity" />
    </application>

</manifest>
