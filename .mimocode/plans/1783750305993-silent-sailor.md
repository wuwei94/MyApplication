# AGENTS.md 重构计划

## 目标
将 AGENTS.md 精简为纯目录索引（50-60 行），将详细内容移到 docs/ 下。

## 当前问题
- AGENTS.md 有 115 行，包含大量文档内容
- 模块注册表表格（17+10 行）已有 docs/modules.md 和 docs/libs.md
- 关键约定详细内容（路由规则、模块结构、全局依赖、Activity 基类、构建命令）应独立为 docs/conventions.md

## 执行步骤

### Step 1: 创建 docs/conventions.md

**文件**: `docs/conventions.md`

将 AGENTS.md 中"关键约定"部分的详细内容移到此文件：
- 路由规则（格式、Main 路由、@Route 注解）
- 模块结构（build.gradle.kts、namespace、resourcePrefix、依赖）
- 全局依赖（AndroidDeps.kt 的基础依赖、测试依赖、功能模块依赖）
- Activity 基类（BasicResponseActivity、BaseVBActivity、BaseFragmentActivity、RouterRecyclerActivity）
- 构建命令（全量构建、单模块构建、--configure-on-demand）

### Step 2: 重构 AGENTS.md

**文件**: `AGENTS.md`

精简为以下结构（目标 50-60 行）：

```markdown
# AGENTS.md

> 本文件是 Agent 知识的目录索引，而非百科全书。
> 控制在 100 行以内，指向更深层的文档来源。

## 项目概述

MyApplication 是一个个人 Android 技术栈沉淀项目，用于展示 Android 开发模式、库和最佳实践。采用 Kotlin、多模块架构和 ARouter 路由导航。

**技术栈**：Kotlin、Jetpack Compose、ARouter、Hilt、Coroutines、Flow、CameraX、Flutter

## 架构

（保留当前的高层架构图）

## 文档目录

| 文档 | 内容 |
|------|------|
| docs/modules.md | 功能模块详情（17 个模块，每个模块的 Activity 列表） |
| docs/libs.md | 库封装详情（10 个库封装的说明） |
| docs/build-logic.md | 构建逻辑（22 个 Convention Plugin 配置详情） |
| docs/conventions.md | 关键约定（路由、模块结构、全局依赖、Activity 基类、构建命令） |

## 不变量

1. 每个 Activity 必须有 @Route 注解
2. 每个模块必须依赖 basic_lib 和 basic_module
3. 新模块必须在 settings.gradle.kts 中注册
4. 资源文件必须使用模块前缀（<模块名>_）
5. 每个 Activity 都应有实际内容，不能是空壳或纯模板代码
```

### Step 3: 验证

1. 检查 AGENTS.md 行数（目标 50-60 行）
2. 检查 docs/conventions.md 内容完整性
3. 检查所有 docs 文件的链接正确性

## 关键文件

- `AGENTS.md` — 重构
- `docs/conventions.md` — 新建
- `docs/modules.md` — 保持不变
- `docs/libs.md` — 保持不变
- `docs/build-logic.md` — 保持不变
