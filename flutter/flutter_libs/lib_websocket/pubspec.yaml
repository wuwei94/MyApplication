name: lib_websocket
description: Flutter WebSocket client wrapper, aligned with Android lib_websocket.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.8

dependencies:
  # https://pub.dev/packages/web_socket_channel
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.26.3
