name: lib_mqtt
description: Flutter MQTT client wrapper, aligned with Android lib_mqtt.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.8

dependencies:
  # https://pub.dev/packages/mqtt_client
  mqtt_client: ^10.11.11

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.26.3
