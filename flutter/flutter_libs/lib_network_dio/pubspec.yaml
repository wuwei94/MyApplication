name: lib_network_dio
description: Dio-based Flutter network client wrapper.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.8

dependencies:
  dio: ^5.9.0

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.26.3
