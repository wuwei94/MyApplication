name: lib_bluetooth
description: Flutter Bluetooth Low Energy (BLE) client package with chunking, queueing, and stream management.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  # https://pub.dev/packages/flutter_blue_plus
  flutter_blue_plus: ^1.35.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
