# `:basic:basic_database`

> 对齐 Google [Now in Android](https://github.com/android/nowinandroid) `:core:database` 设计：
> 独立 Room 本地持久化数据库模块。包含 Room 实体（`ArticleEntity`）、数据访问对象（`ArticleDao`）、数据库实例（`ArticleDatabase`）与本地数据源契约实现（`ArticleLocalDataSource`）。
