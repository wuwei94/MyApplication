name: lib_event_bus
description: Flutter event bus wrapper, aligned with Android lib_eventbus.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.8

dependencies:
  # https://pub.dev/packages/event_bus
  event_bus: ^2.0.1

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.26.3
