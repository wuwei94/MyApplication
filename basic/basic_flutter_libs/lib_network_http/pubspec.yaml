name: lib_network_http
description: package:http-based Flutter network client wrapper.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.8

dependencies:
  async: ^2.13.0
  http: ^1.6.0
  logger: ^2.6.1

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.26.3
